# You can put all your user choices here

TEST_ENV = "DEFAULT"  # Should be one of [DEFAULT]
BROWSER_TYPE = "CHROME" # Should be one of [CHROME, EDGE, FIREFOX, SAFARI, CHROME_HEADLESS, FIREFOX_HEADLESS]
